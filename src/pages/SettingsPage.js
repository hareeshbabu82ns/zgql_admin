import { Container } from "@mui/material"

const SettingsPage = () => {
  return (
    <Container maxWidth="lg" sx={{ mt: 4, mb: 4 }}>
      Settings
    </Container>
  );
};

export default SettingsPage
